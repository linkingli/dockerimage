```
CMP: E1157
PLAT:CloudOS-PLAT-E5102-V500R001B01D030SP01-RC1
```
```
https://bbs.wosign.com/thread-3526-1-1.html

```

```
	//过滤标签
	hostFlag := request.Header.Get("isHost")
	syncFlag := request.Header.Get("isSync")
	//true:存在host，flase:存在container，
	//默认放在host
	switch hostFlag {
	case "false":
	default:

	}
	//true:同步所有节点，false:不同步，controller:同步到controller,work:同步到worker节点
	//默认同步到所有节点
	switch syncFlag {
	case "false":
	case "controller":
	case "worker":
	default:

	}

```
